services:
  app:
    build:
      context: .
    image: threejs-robotics-arms:dev
    ports:
      - "${APP_PORT:-5174}:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      CHOKIDAR_USEPOLLING: "true"

volumes:
  node_modules:
